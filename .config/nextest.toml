# cargo-nextest 配置
[profile.default]
retries = 0
failure-output = "immediate"
fail-fast = true

[profile.ci]
retries = 2
failure-output = "immediate-final"
fail-fast = false
slow-timeout = { period = "60s", terminate-after = 3 }
