//! `umbra` CLI 入口（子命令：`server` / `client` / `keygen`）。
//!
//! 脚手架占位，**尚未实现**。请遵循 SDD：任何实现前先在 `openspec/` 提出并批准变更（见根 `AGENTS.md`）。
//! 协议规范见 `docs/protocol-design.md`；OpenSpec 能力：`cli`。
fn main() {
    eprintln!("umbra: 脚手架占位，尚未实现。请遵循 SDD 工作流（见 AGENTS.md 与 openspec/）。");
}
