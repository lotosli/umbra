---
id: "reference/protocol"
title: "Comparar solucions de servidor intermediari"
description: "Compara desplegament, transports i integració amb aplicacions."
section: "reference"
order: 16
version: "1.0.0-alpha"
source: ["README.md", "docs/usage.md", "docs/performance.md", "crates/umbra-core/src/runtime.rs", "crates/umbra-reality/src/prebuild.rs", "crates/umbra-reality/src/cert.rs", "crates/umbra-tls/src/server.rs"]
translation: "complete"
---

Parteix de la xarxa, del manteniment que vols assumir i de les aplicacions. Umbra reuneix cobertura web, TCP/Vision, QUIC i SOCKS5. Es compara Umbra 1.0.0-alpha; Xray es refereix a VLESS + REALITY + Vision per TCP.

## Desplegament i transport

| Solució | Característiques | Desplegament | TCP i UDP |
| --- | --- | --- | --- |
| Umbra | Cobertura web i una entrada local | Dos extrems, claus i lloc; sense certificat CA del node | Mux TCP o Vision; QUIC separat per a UDP |
| Xray + VLESS + REALITY + Vision | REALITY i Vision per TCP | Configurar Xray, identitat, lloc i client compatible | Exterior TCP aquí; UDP segons flow i client |
| VMess AEAD | Protocol xifrat amb transports a triar | Client i transport compatibles; identitat TLS si s’afegeix | Peticions TCP/UDP; transport segons configuració |
| Trojan | TLS amb retorn a un lloc | Habitualment domini, certificat i clau privada | TCP i UDP dins de TLS/TCP |
| Shadowsocks AEAD / 2022 | Intermediari xifrat amb claus | Claus i clients compatibles; connectors a part | TCP/UDP natius; verificar cada connector |
| Hysteria 2 | QUIC; lloc HTTP/3 abans d’autenticar | UDP accessible i configuració TLS | TCP en fluxos fiables; UDP en datagrames no fiables |

## Què simplifica Umbra

Amb `transport = "tcp"`, `udp_transport = "quic"` i `mux = false`, una entrada local porta TCP per Vision i UDP per QUIC. N'hi ha prou amb un procés a cada extrem. Les claus i el certificat temporal eviten sol·licitar i renovar un certificat públic per al node. Mantén les claus i un lloc real accessible, amb QUIC si l'utilitzes.

SOCKS5 permet conservar aplicacions i regles. Mux redueix obertures repetides; Vision redueix xifratge extern per a TLS 1.3 apte.

## Segons l'entorn

Amb Xray/REALITY, compara rutes, clients i manteniment amb l'entrada dual d'Umbra. Amb VMess, revisa el transport abans de migrar. Trojan ofereix desplegament TLS convencional; Shadowsocks, un intermediari xifrat basat en claus i connectors segons les necessitats. Per a QUIC, compara Hysteria 2 amb les teves aplicacions UDP. Umbra és una CLI Alpha: considera també interfície gràfica, mòbil i subscripcions si et calen.

## Una diferència important per a UDP

Hysteria 2 utilitza datagrames no fiables. Umbra fa servir fluxos QUIC fiables per a les associacions UDP: les dades perdudes poden retardar les següents. Compara latència i pèrdues en trucades, jocs i altres usos en temps real.

## Més informació

L'arquitectura explica el recorregut i el model de seguretat els perfils i la negociació criptogràfica. Geneva DSL, 0-RTT i preconnexió de reserva no estan disponibles. Compara velocitat i estabilitat amb la mateixa càrrega i ruta.

[Xray](https://xtls.github.io/config/outbounds/vless.html) · [VMess](https://www.v2fly.org/developer/protocols/vmess.html) · [Trojan](https://trojan-gfw.github.io/trojan/protocol) · [Shadowsocks](https://shadowsocks.org/doc/sip022.html) · [Hysteria 2](https://v2.hysteria.network/docs/developers/Protocol/)

## Passos següents

- [Coneix Umbra](/ca/docs/getting-started/introduction/)
- [Triar TCP, Vision o QUIC](/ca/docs/concepts/transports/)
- [Connectar aplicacions i clients compatibles amb Clash](/ca/docs/guides/client/)
- [Model de seguretat](/ca/docs/concepts/security-model/)
