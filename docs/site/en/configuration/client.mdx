---
id: "configuration/client"
title: "Client configuration"
description: "Look up server identity, transport and local proxy settings."
section: "configuration"
order: 9
version: "1.0.0-alpha"
source: ["crates/umbra-core/src/config.rs", "README.md", "crates/umbra/src/main.rs"]
translation: "complete"
---

The client TOML defines the remote endpoint and the local SOCKS5 proxy.

## Minimal configuration

Replace the server address, website name and identity placeholders:

```toml
server = "198.51.100.10:443"
transport = "tcp"
public_key = "<X25519_PUBLIC_BASE64>"
short_id = "<SHORT_ID_HEX>"
server_name = "cover.example"
fingerprint = "chrome-latest"
mldsa_verify = "<MLDSA_VERIFY_BASE64>"
socks_listen = "127.0.0.1:1080"
```

## Fields

| Field | Default / requirement | Meaning |
|---|---|---|
| `server` | Required | Umbra server, host:port |
| `transport` | Required | Main outer transport: tcp or quic |
| `udp_transport` | `transport` | UDP outer transport; inherits transport if omitted |
| `public_key` | Required | Server X25519 public identity, 32 base64-decoded bytes |
| `short_id` | Required | One accepted server short ID |
| `server_name` | Required | Accepted server SNI |
| `fingerprint` | Required | Built-in fingerprint profile name |
| `mldsa_verify` | Required | Server ML-DSA verification key, nonempty base64 |
| `socks_listen` | Required | Local SOCKS5 socket, no-auth |
| `spider_path` | `"/"` | Real-site path; begins with /, no CR/LF |
| `mux` | `true` | Encrypted mux; false selects dedicated TCP/Vision |
| `padding_scheme` | `"default"` | Inner padding policy |
| `tcp_evasion` | `"segment"` | Ordered ClientHello write policy |

## Match the server identity

`public_key` is the server's X25519 public key, decoding to 32 bytes. `mldsa_verify` is the corresponding non-empty Base64 verification key from `keygen`. Obtain both through a trusted channel. Match `short_id` to a server entry and `server_name` to an allowed website name.

## Select transports

`transport` is required and accepts `tcp` or `quic`. UDP inherits the final merged value unless `udp_transport` is set explicitly. The latter changes only UDP requests. TCP uses `mux = true` by default; false selects dedicated Vision connections.

## Local access and browser profiles

Bind `socks_listen` to `127.0.0.1:1080` because the proxy has no password authentication. `fingerprint` selects the handshake profile. The built-in `chrome-latest` and `chrome-150-macos` currently both use Chrome 150; profile aliases change with software releases.

`spider_path` defaults to `/` and is used for real-site behavior when an Umbra session is not authenticated. It must begin with `/` and contain no carriage return or newline.

CLI values override file fields before validation. Keep performance settings in `[performance]`; use defaults until the connection works.

## Next steps

- [Connect apps and Clash-compatible clients](/en/docs/guides/client/)
- [Performance and transport settings](/en/docs/reference/configuration/)
- [Command-line reference](/en/docs/reference/cli/)
