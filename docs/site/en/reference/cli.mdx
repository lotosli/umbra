---
id: "reference/cli"
title: "Command-line reference"
description: "Find options for server, client and keygen."
section: "reference"
order: 14
version: "1.0.0-alpha"
source: ["crates/umbra/src/main.rs", "README.md"]
translation: "complete"
updatedAt: "2026-09-18"
reviewedAt: "2026-09-18"
---

`server` starts the server, `client` starts the local SOCKS5 proxy, and `keygen` generates identity keys.

## Get help

```sh
umbra --help
umbra server --help
umbra client --help
umbra keygen
```

Use each installed command's `--help` for its supported options.

## Read and override configuration

Use `-c` or `--config` for TOML. Explicit CLI options override matching fields; omitted values stay as configured. Server short IDs and names accept comma-separated lists. Boolean options such as `--mux` and `--prebuild` need `true` or `false`. Put performance settings in the TOML `[performance]` table.

## Server options

| Command / option | Purpose |
|---|---|
| `-c, --config` | `server.toml` |
| `--listen` | TCP socket bind address |
| `--udp-listen` | Override the QUIC UDP listener address |
| `--private-key` | X25519 private key, base64, exactly 32 bytes |
| `--short-ids` | Nonempty list of hex IDs, each 0–8 bytes |
| `--dest` | Real fallback destination, host:port |
| `--server-names` | Nonempty accepted SNI list |
| `--max-time-diff` | Allowed timestamp skew, at least 1 second |
| `--mldsa-seed` | ML-DSA signing seed, base64, exactly 32 bytes |
| `--prebuild` | Periodic destination refresh; startup probe always runs |
| `--padding-scheme` | Inner padding policy |
| `--tcp-evasion` | Ordered ClientHello write policy |

## Client options

| Command / option | Purpose |
|---|---|
| `-c, --config` | `client.toml` |
| `--server` | Umbra server, host:port |
| `--transport` | Main outer transport: tcp or quic |
| `--udp-transport` | Override the UDP transport: tcp or quic |
| `--public-key` | Server X25519 public identity, 32 base64-decoded bytes |
| `--short-id` | One accepted server short ID |
| `--server-name` | Accepted server SNI |
| `--fingerprint` | Built-in fingerprint profile name |
| `--mldsa-verify` | Server ML-DSA verification key, nonempty base64 |
| `--socks-listen` | Local SOCKS5 socket, no-auth |
| `--spider-path` | Real-site path; begins with /, no CR/LF |
| `--mux` | Encrypted mux; false selects dedicated TCP/Vision |
| `--padding-scheme` | Inner padding policy |
| `--tcp-evasion` | Ordered ClientHello write policy |

## Generate keys

`umbra keygen` takes no extra options and outputs four Base64 values: `x25519_private`, `x25519_public`, `mldsa_seed` and `mldsa_verify`. Assign them as shown in the quick start. The output contains secrets; save it in a protected file and prefer configuration files over secret command-line arguments.

## Next steps

- [Server configuration](/en/docs/configuration/server/)
- [Client configuration](/en/docs/configuration/client/)
- [Performance and transport settings](/en/docs/reference/configuration/)
