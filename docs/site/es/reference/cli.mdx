---
id: "reference/cli"
title: "Referencia de la línea de comandos"
description: "Consulta las opciones de server, client y keygen."
section: "reference"
order: 14
version: "1.0.0-alpha"
source: ["crates/umbra/src/main.rs", "README.md"]
translation: "complete"
updatedAt: "2026-09-18"
reviewedAt: "2026-09-18"
---

`server` inicia el servidor, `client` el proxy SOCKS5 local y `keygen` genera claves.

## Ver la ayuda

```sh
umbra --help
umbra server --help
umbra client --help
umbra keygen
```

Consulta `--help` en la versión instalada.

## Cargar y sustituir configuración

`-c` o `--config` carga TOML. Las opciones indicadas reemplazan sus campos; los demás se conservan. Las listas de identificadores y nombres del servidor se separan por comas. `--mux` y `--prebuild` requieren `true` o `false`. El rendimiento se configura en `[performance]`.

## Opciones del servidor

| Comando / opción | Función |
|---|---|
| `-c, --config` | `server.toml` |
| `--listen` | Dirección numérica de escucha TCP |
| `--udp-listen` | Reemplazar la dirección de escucha QUIC UDP |
| `--private-key` | Clave privada X25519, base64, exactamente 32 bytes |
| `--short-ids` | Lista no vacía de ID hexadecimales de 0–8 bytes |
| `--dest` | Destino real de respaldo, host:puerto |
| `--server-names` | Lista no vacía de SNI admitidos |
| `--max-time-diff` | Desfase permitido, al menos un segundo |
| `--mldsa-seed` | Semilla ML-DSA, base64, exactamente 32 bytes |
| `--prebuild` | Actualización periódica; comprobación inicial obligatoria |
| `--padding-scheme` | Política de relleno interno |
| `--tcp-evasion` | Escrituras ordenadas de ClientHello |

## Opciones del cliente

| Comando / opción | Función |
|---|---|
| `-c, --config` | `client.toml` |
| `--server` | Servidor Umbra, host:puerto |
| `--transport` | Transporte principal: tcp o quic |
| `--udp-transport` | Reemplazar el transporte UDP: tcp o quic |
| `--public-key` | Identidad pública X25519, 32 bytes decodificados |
| `--short-id` | Un short ID admitido por el servidor |
| `--server-name` | SNI admitido por el servidor |
| `--fingerprint` | Nombre de perfil de huella incorporado |
| `--mldsa-verify` | Clave de verificación ML-DSA, base64 no vacío |
| `--socks-listen` | Escucha SOCKS5 local, sin autenticación |
| `--spider-path` | Ruta del sitio real; empieza por /, sin CR/LF |
| `--mux` | Mux cifrado; false elige TCP/Vision dedicado |
| `--padding-scheme` | Política de relleno interno |
| `--tcp-evasion` | Escrituras ordenadas de ClientHello |

## Generar claves

`umbra keygen` no necesita opciones adicionales y devuelve `x25519_private`, `x25519_public`, `mldsa_seed` y `mldsa_verify` en Base64. Distribúyelos según el inicio rápido. La salida contiene secretos: guárdala en un archivo protegido y evita argumentos que queden en el historial o la lista de procesos.

## Siguientes pasos

- [Referencia de configuración del servidor](/es/docs/configuration/server/)
- [Referencia de configuración del cliente](/es/docs/configuration/client/)
- [Parámetros de rendimiento y transporte](/es/docs/reference/configuration/)
