---
id: "reference/protocol"
title: "Comparer les solutions proxy"
description: "Comparez déploiement, transports et intégration aux applications."
section: "reference"
order: 16
version: "1.0.0-alpha"
source: ["README.md", "docs/usage.md", "docs/performance.md", "crates/umbra-core/src/runtime.rs", "crates/umbra-reality/src/prebuild.rs", "crates/umbra-reality/src/cert.rs", "crates/umbra-tls/src/server.rs"]
translation: "complete"
updatedAt: "2026-09-18"
reviewedAt: "2026-09-18"
---

Partez du réseau disponible, de l'entretien souhaité et de vos applications. Umbra regroupe couverture web, TCP/Vision, QUIC et SOCKS5 dans une paire client/serveur. Cette comparaison porte sur Umbra 1.0.0-alpha ; Xray désigne ici VLESS + REALITY + Vision sur TCP.


Contenu vérifié：2026-09-18.

## Déploiement et transport

| Solution | Caractéristiques | Déploiement | TCP et UDP |
| --- | --- | --- | --- |
| Umbra | Couverture web ; une entrée locale | Installez la même version d’Umbra aux deux extrémités et lancez-la en ligne de commande. QUIC nécessite un port UDP ouvert et un site de couverture compatible QUIC. | Mux TCP ou Vision ; QUIC distinct possible pour UDP |
| Xray + VLESS + REALITY + Vision | REALITY et Vision sur TCP | Utilisez le même identifiant utilisateur aux deux extrémités. Dans le client, renseignez les paramètres d’identité REALITY du serveur et réglez flow sur xtls-rprx-vision. | TCP externe ici ; UDP selon flow et client |
| VMess AEAD | Protocole chiffré, transports au choix | Utilisez le même identifiant et le même transport des deux côtés. Avec WebSocket, alignez le chemin ; avec TLS, configurez le domaine et le certificat. | Requêtes TCP/UDP ; transport selon configuration |
| Trojan | TLS avec repli vers un site | Configurez le domaine du serveur, un certificat valide et un mot de passe, puis saisissez ce mot de passe dans le client. Le protocole Trojan standard transporte aussi les requêtes UDP dans une connexion TCP. | TCP et UDP dans TLS/TCP |
| Shadowsocks AEAD / 2022 | Proxy chiffré par clés | Utilisez la même méthode de chiffrement et la même clé des deux côtés ; ouvrez le port UDP si nécessaire. Configurez les éventuels modules complémentaires selon leurs instructions. | TCP/UDP natifs ; vérifier les plugins |
| Hysteria 2 | QUIC ; site HTTP/3 avant authentification | Ouvrez le port UDP du serveur et configurez le certificat TLS et l’authentification. Si le réseau bloque UDP, choisissez une autre méthode de connexion. | TCP en flux fiables ; UDP en datagrammes non fiables |

## Ce qu'Umbra simplifie

Avec `transport = "tcp"`, `udp_transport = "quic"` et `mux = false`, une seule entrée locale transporte TCP via Vision et UDP via QUIC. Un processus suffit à chaque extrémité. Les clés et le certificat temporaire évitent de demander et renouveler un certificat public pour le nœud. Maintenez les clés et un site réel accessible, compatible QUIC si nécessaire.

SOCKS5 permet de conserver applications et client de règles. Le mux réduit les ouvertures répétées ; Vision réduit le chiffrement externe des données TLS 1.3 admissibles.

## Selon votre environnement

Pour Xray/REALITY, comparez vos besoins de routage et de clients avec l'entrée unique à deux transports d'Umbra. Avec VMess, vérifiez la combinaison de transport avant migration. Trojan convient à l'étude d'un déploiement TLS classique ; Shadowsocks à un proxy chiffré configuré par clés, avec plugins selon les besoins. Pour QUIC, comparez Hysteria 2 sur vos applications UDP réelles. Umbra reste une CLI Alpha : tenez compte de vos besoins d'interface graphique, de mobile ou d'abonnements.

## Différence importante pour UDP

Hysteria 2 utilise des datagrammes non fiables. Umbra emploie des flux QUIC fiables pour les associations UDP : une donnée manquante peut retarder la suite du flux. Comparez latence et pertes pour appels, jeux et autres usages temps réel.

## Pour aller plus loin

L'architecture décrit le parcours des connexions ; le modèle de sécurité détaille profils et négociation cryptographique. Geneva DSL, 0-RTT et préconnexion de repli ne sont pas disponibles. Comparez débit et stabilité à charge et trajet identiques.

[Xray](https://xtls.github.io/config/outbounds/vless.html) · [VMess](https://www.v2fly.org/developer/protocols/vmess.html) · [Trojan](https://trojan-gfw.github.io/trojan/protocol) · [Shadowsocks](https://shadowsocks.org/doc/sip022.html) · [Hysteria 2](https://v2.hysteria.network/docs/developers/Protocol/)

## Pour continuer

- [Découvrir Umbra](/fr/docs/getting-started/introduction/)
- [Choisir TCP, Vision ou QUIC](/fr/docs/concepts/transports/)
- [Connecter les applications et les clients de type Clash](/fr/docs/guides/client/)
- [Modèle de sécurité](/fr/docs/concepts/security-model/)

- [Conception du protocole](/fr/docs/reference/protocol-design/)
