---
id: "reference/cli"
title: "コマンドラインリファレンス"
description: "server、client、keygen のオプションを確認します。"
section: "reference"
order: 14
version: "1.0.0-alpha"
source: ["crates/umbra/src/main.rs", "README.md"]
translation: "complete"
updatedAt: "2026-09-18"
reviewedAt: "2026-09-18"
---

`server` はサーバー、`client` はローカル SOCKS5 を起動し、`keygen` は鍵を生成します。

## ヘルプを表示

```sh
umbra --help
umbra server --help
umbra client --help
umbra keygen
```

インストールした版の `--help` を確認してください。

## 設定の読み込みと上書き

`-c` または `--config` で TOML を読み込みます。指定したオプションだけ対応値を上書きします。サーバーの ID と名前のリストはコンマ区切りです。`--mux` と `--prebuild` は `true` または `false` が必要です。性能設定は `[performance]` に置きます。

## サーバーのオプション

| コマンド / オプション | 役割 |
|---|---|
| `-c, --config` | `server.toml` |
| `--listen` | 数値 IP の TCP 待ち受け |
| `--udp-listen` | 設定ファイルの QUIC UDP 待ち受けを上書き |
| `--private-key` | X25519 秘密鍵。base64 復号後32バイト |
| `--short-ids` | 0〜8バイトの16進 ID。リストは1件以上 |
| `--dest` | 実際のフォールバック先 host:port |
| `--server-names` | 許可する SNI のリスト。空は不可 |
| `--max-time-diff` | 時刻の許容差。最低1秒 |
| `--mldsa-seed` | ML-DSA 署名シード。base64 復号後32バイト |
| `--prebuild` | 接続先の定期更新。起動検査は常に実行 |
| `--padding-scheme` | 内部パディング方式 |
| `--tcp-evasion` | ClientHello の順序付き分割 write |

## クライアントのオプション

| コマンド / オプション | 役割 |
|---|---|
| `-c, --config` | `client.toml` |
| `--server` | Umbra サーバー host:port |
| `--transport` | 主要な外側方式。tcp または quic |
| `--udp-transport` | UDP 方式を上書き。tcp または quic |
| `--public-key` | サーバー X25519 公開鍵。復号後32バイト |
| `--short-id` | サーバーが許可する short ID 一つ |
| `--server-name` | サーバーが許可する SNI |
| `--fingerprint` | 組み込みフィンガープリントの名前 |
| `--mldsa-verify` | サーバー ML-DSA 検証鍵。空でない base64 |
| `--socks-listen` | 認証なしのローカル SOCKS5 待ち受け |
| `--spider-path` | 実サイトのパス。先頭は /、CR/LF 不可 |
| `--mux` | 暗号化 mux。false は専用 TCP/Vision |
| `--padding-scheme` | 内部パディング方式 |
| `--tcp-evasion` | ClientHello の順序付き分割 write |

## 鍵を生成

`umbra keygen` は追加オプションなしで `x25519_private`、`x25519_public`、`mldsa_seed`、`mldsa_verify` を Base64 で出力します。クイックスタートに従って割り当ててください。秘密を含むため保護されたファイルに保存し、履歴やプロセス一覧に出る引数は避けます。

## 次のステップ

- [サーバー設定リファレンス](/ja/docs/configuration/server/)
- [クライアント設定リファレンス](/ja/docs/configuration/client/)
- [性能と通信パラメーター](/ja/docs/reference/configuration/)
