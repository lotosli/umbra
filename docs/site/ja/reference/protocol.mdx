---
id: "reference/protocol"
title: "プロキシ構成を比較する"
description: "構築方法、通信方式、アプリとの連携を比較します。"
section: "reference"
order: 16
version: "1.0.0-alpha"
source: ["README.md", "docs/usage.md", "docs/performance.md", "crates/umbra-core/src/runtime.rs", "crates/umbra-reality/src/prebuild.rs", "crates/umbra-reality/src/cert.rs", "crates/umbra-tls/src/server.rs"]
translation: "complete"
---

利用できるネットワーク、管理方法、既存アプリから選びます。Umbra はサイトによるカバー、TCP/Vision、QUIC、SOCKS5 を組み合わせます。対象は Umbra 1.0.0-alpha で、Xray は TCP 上の VLESS + REALITY + Vision を指します。

## 構築と通信方式

| 構成 | 主な特徴 | 構築 | TCP と UDP |
| --- | --- | --- | --- |
| Umbra | 実在サイトのカバーと一つのローカル入口 | 両端、鍵、サイトを管理。ノード用 CA 証明書は不要 | TCP mux または Vision。UDP は別途 QUIC を選択可能 |
| Xray + VLESS + REALITY + Vision | TCP 上の REALITY と Vision | Xray、認証情報、サイト、対応クライアントを設定 | ここでは外側 TCP。UDP は flow とクライアントに依存 |
| VMess AEAD | 通信方式を選べる暗号化プロトコル | 対応クライアントと伝送を用意。TLS 追加時は認証も設定 | TCP/UDP 要求。外側は設定に従う |
| Trojan | TLS とサイトへのフォールバック | 通常はドメイン、証明書、秘密鍵を管理 | TCP と UDP を TLS/TCP 上で伝送 |
| Shadowsocks AEAD / 2022 | 鍵による暗号化プロキシ | 対応する鍵とクライアント。プラグインは別設定 | 基本は TCP/UDP。プラグインの対応を個別確認 |
| Hysteria 2 | QUIC。未認証時は HTTP/3 サイトとして応答 | 到達可能な UDP と TLS 設定 | TCP は信頼性ストリーム、UDP は非信頼性データグラム |

## Umbra でまとめられること

`transport = "tcp"`、`udp_transport = "quic"`、`mux = false` で TCP は Vision、UDP は QUIC にし、一つのローカル入口を使えます。各端で一つのプロセスが動きます。鍵と一時証明書によりノード用の公開 CA 証明書の取得・更新を省けます。鍵と到達可能なサイトを管理し、QUIC 利用時はサイトも対応させます。

SOCKS5 で既存アプリとルールを維持できます。mux は再接続を、Vision は適用可能な TLS 1.3 の外側の暗号化を減らします。

## 既存環境から選ぶ

Xray/REALITY では現在のルーティング、クライアント、管理方法と Umbra の一入口・二経路構成を比べます。VMess は移行前に通信構成を確認します。Trojan は一般的な TLS 構築、Shadowsocks は鍵と必要なプラグインによる暗号化プロキシを検討できます。QUIC では実際の UDP アプリで Hysteria 2 と比較してください。Umbra は Alpha の CLI なので、GUI、モバイル、サブスクリプション管理の要否も考慮します。

## UDP の重要な違い

Hysteria 2 は非信頼性データグラム、Umbra は信頼性 QUIC ストリームで UDP 関連付けを運びます。後者では欠けたデータが後続を待たせることがあります。通話やゲームでは実際の経路で遅延と損失を比較してください。

## 詳細

接続の流れはアーキテクチャ、プロファイルと暗号協議はセキュリティモデルを参照してください。Geneva DSL、0-RTT、フォールバック先への事前接続は利用できません。速度と安定性は同じ負荷と経路で比較します。

[Xray](https://xtls.github.io/config/outbounds/vless.html) · [VMess](https://www.v2fly.org/developer/protocols/vmess.html) · [Trojan](https://trojan-gfw.github.io/trojan/protocol) · [Shadowsocks](https://shadowsocks.org/doc/sip022.html) · [Hysteria 2](https://v2.hysteria.network/docs/developers/Protocol/)

## 次のステップ

- [Umbra とは](/ja/docs/getting-started/introduction/)
- [TCP・Vision・QUIC の選び方](/ja/docs/concepts/transports/)
- [アプリや Clash 系クライアントを接続する](/ja/docs/guides/client/)
- [セキュリティモデル](/ja/docs/concepts/security-model/)
