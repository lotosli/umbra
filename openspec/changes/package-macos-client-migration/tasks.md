## 1. Scope and inputs
- [x] 1.1 Record the explicitly authorized private, script-based migration scope before implementation.
- [x] 1.2 Locate the installed alpha binaries, client configuration and two selected self-contained Clash profiles without printing credentials.

## 2. Packaging
- [x] 2.1 Add destination-user installation/control scripts with backups, rollback, login startup and no additional runtime dependencies.
- [x] 2.2 Assemble an ad-hoc signed universal Mac binary, private configuration copies, controls and Chinese instructions on the desktop.
- [x] 2.3 Package the directory as a transferable ZIP, retain file permissions and record static integrity/structure checks without running installation or additional throughput tests.

## 3. Delivery
- [x] 3.1 Provide desktop links and concise destination installation/import steps; preserve the current running services and keep the private bundle out of public releases and git.
