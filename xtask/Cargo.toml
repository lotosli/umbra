[package]
name = "xtask"
description = "开发/CI 辅助任务运行器（仅 std）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# 开发工具：不套用 workspace 的 pedantic lints

[dependencies]
